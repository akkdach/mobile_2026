export interface AppBarInterface {
  title: string;
  subtitle?: string;
  rightTitle?: string;
  onBackReload?: boolean;
  hideBackIcon?: boolean;
  replacePath?: string;
  replaceProps?: any;
  pageValueChange?: boolean
}
